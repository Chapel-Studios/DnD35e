import { Material } from '@effects/material/Material.mjs';
import { Secret } from '@effects/secret/Secret.mjs';

const EffectConfig = {
  documentClasses: {
    material: Material,
    secret: Secret,
  },
};

export { EffectConfig };
