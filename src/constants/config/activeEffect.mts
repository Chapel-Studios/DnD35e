import { Material } from '@effects/material/index.mjs';
import { Secret } from '@effects/secret/index.mjs';

const EffectConfig = {
  documentClasses: {
    material: Material,
    secret: Secret,
  },
};

export { EffectConfig };
