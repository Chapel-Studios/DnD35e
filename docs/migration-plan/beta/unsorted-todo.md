# Unsorted TODOs

Ideas and deferred work not yet assigned to a specific phase. Pull items into a real phase doc when scoped.

- [ ] Combat tracker: active-combatant mini stat-block panel (HP editable, AC/BAB/saves summary, attacks, feats) shown below the tracker list for the current turn's combatant — see `CombatTrackerDnd35e.mts` for the Vue-owned tracker it should slot into. D35E precedent: `ActorSheetPFNPCCombat` (`module/actor/sheets/npc-combat.js` + `combat.js`), single-instance panel swapped per turn, gated behind a `useCombatCharacterSheet`-style setting.

- [ ] Combat tracker: surprise rounds!